spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/arppl4_spring_rental?serverTimezone=Europe/Warsaw&createDatabaseIfNotExist=true
  jpa:
    hibernate:
      ddl-auto: update
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
